#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path("../lib", __dir__)

require "relay/server"

Relay::Server.new(
  control_port: 7777,
  public_port: 8080,
  domain_suffix: ".localhost:8080",
  tls_cert: ENV["RELAY_TLS_CERT"],
  tls_key: ENV["RELAY_TLS_KEY"]
).start
